{
  "name": "ssh-helper-terminal",
  "private": true,
  "scripts": {
    "build": "esbuild src/index.js --bundle --minify --format=iife --outfile=../app/src/main/assets/terminal/terminal.js"
  },
  "dependencies": {
    "@xterm/addon-fit": "0.11.0",
    "@xterm/addon-search": "0.16.0",
    "@xterm/addon-web-links": "0.12.0",
    "@xterm/xterm": "6.0.0"
  },
  "devDependencies": {
    "esbuild": "0.25.9"
  }
}
